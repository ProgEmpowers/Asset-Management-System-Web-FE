import { FilterAssetByStatusPipe } from './filter-asset-by-status.pipe';

describe('FilterAssetByStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAssetByStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
