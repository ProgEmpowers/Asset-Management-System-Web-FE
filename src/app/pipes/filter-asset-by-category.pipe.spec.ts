import { FilterAssetByCategoryPipe } from './filter-asset-by-category.pipe';

describe('FilterAssetByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAssetByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
